<div class="container">
  <div class="row">
<% @notes.each do |note| %>
<div class="wrapper_with_padding">
<h2><%= link_to note.title, note %></h2>
<p><%= time_ago_in_words(note.created_at) %></p>
<% end %>
</div>

<h4><%= link_to "New Note", new_note_path %></h2>
</div>
</div>
